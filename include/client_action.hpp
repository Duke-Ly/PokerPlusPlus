//
// client_action.hpp
//

#ifndef CLIENT_ACTION_HPP
#define CLIENT_ACTION_HPP

class client_action
{
public:
    bool check;
    bool call;
    bool fold;
    bool bet;
    bool raise;
    bool replaceCards;
};

#endif
